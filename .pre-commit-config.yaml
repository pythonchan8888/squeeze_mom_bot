repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.0        # pin a specific Ruff tag
    hooks:
      - id: ruff       # runs `ruff check .`
      - id: ruff-format  # runs `ruff format .` (auto-fix)